@{
    ViewData["Title"] = "Categorias";
}

@model List<SistemaVenda.Models.CategoriaViewModel>;

<h2>@ViewData["Title"]</h2>
<hr />

<table class="table table-bordered">
    <thead>
        <tr style="background-color:#f6f6f6">
            <th width="20%">Código</th>
            <th width="80%">Descrição</th>
        </tr>
    </thead>
    <tbody>
        @foreach(var item in Model)
        {
            <tr onclick="Editar(@item.Codigo)" style="cursor:pointer";>
                <td>@item.Codigo</td>
                <td>@item.Descricao</td>
            </tr>
        }
    </tbody>
</table>
<br />
<button type="button" class="btn btn-block btn-primary" onclick="Cadastrar()">Cadastrar Categoria</button>

<script>
    function Editar(codigo) {
        Request("Cadastro\\" + codigo);
    }
    function Cadastrar() {
        Request("Cadastro");
    }
    function Request(request){
        window.location = window.origin + "\\Categoria\\" + request;
    }
</script>
